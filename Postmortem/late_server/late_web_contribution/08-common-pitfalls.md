# 08. Common Pitfalls

- assuming `late-web` owns all browser behavior
- changing websocket URL behavior without checking `late-ssh`
- breaking `/stream` fallback behavior
- treating DB-backed pages like they are realtime pages, or vice versa
- forgetting Askama template changes may depend on config/runtime values
- missing `late-web/CONTEXT.md` invariants
