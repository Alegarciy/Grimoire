# 08. Common Pitfalls

- breaking token handshake compatibility
- forgetting mode-specific behavior differences
- changing analyzer logic so it no longer reflects audible output
- breaking websocket URL rewriting behavior
- missing platform-specific quirks like WSL or Android handling
- assuming OpenSSH mode behaves like native mode
